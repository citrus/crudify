<h3>Edit Jelly</h3>

<%= render 'shared/error_messages', :target => @jelly %>

<%= form_for @jelly do |f| %>
  <%= render 'fields', :form => f %>
  <p class="action">
    <%= f.submit 'update' %>
    or <%= link_to "cancel", jellies_path %>
  </p>
<% end %>